server:
  port: 10201
  
spring:
  application:
    name: library-book
  datasource:
    url: jdbc:mysql://104.197.25.115/book
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: book
    password: book

  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: update